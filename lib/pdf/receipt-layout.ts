/** Hauteur minimale réservée pour le bloc de clôture (références + mention + footer). */
export const RECEIPT_CLOSING_MIN_PRESENCE = 132;

/** Hauteur minimale pour la rangée client / professionnel. */
export const RECEIPT_PARTIES_MIN_PRESENCE = 88;
